/* The std OpenAL module simply provides both the ALC and AL modules. It's
 * intended to provide just the standard interface, without any extensions.
 */

export module openal.std;

export import openal.alc;
export import openal.al;
